// Public runtime configuration only.
// Environment IDs and function names are public identifiers, not secrets.
// Never put API keys, CloudBase secrets, private repository tokens, or customer data here.
window.TRAVEL_GUIDE_CONFIG = Object.freeze({
  apiBase: "",
  demoUrl: "./demo.json",
  cloudbaseEnvId: "travel-planner-d9gm7wp2b5b69c288",
  cloudbaseRegion: "ap-shanghai",
  cloudbaseFunctionName: "travel-planner"
});

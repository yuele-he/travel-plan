// Public runtime configuration only.
// Never put API keys, CloudBase secrets, private repository tokens, or customer data here.
window.TRAVEL_GUIDE_CONFIG = Object.freeze({
  apiBase: "",
  demoUrl: "./demo.json"
});
